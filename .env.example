# CoinGecko Pro API key (optional).
# Leave unset to use the free public endpoint (same key as pintuwang/Crypto).
# Store in GitHub: Settings → Secrets and variables → Actions → COINGECKO_API_KEY
COINGECKO_API_KEY=
